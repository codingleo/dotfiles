---
name: ux-panel
description: >
  Legacy alias for /uxui-panel — adversarial 10-agent UX/UI panel (5 UX lenses + 5 UI design
  experts). Prefer the name uxui-panel. Use when the user runs /ux-panel or asks for a multi-agent
  UX panel debate on a UI surface.
---

# UX Panel → UX/UI Panel (renamed)

This skill was **renamed to `uxui-panel`** (`/uxui-panel`).

**Do not use this file as the source of truth.** Load and follow:

**`uxui-panel`** — path: sibling skill `../uxui-panel/SKILL.md`  
(or absolute: the `uxui-panel` skill in the skills directory).

## What changed

- Panel is now **10 agents by default**: 5 UX lenses + 5 UI design experts  
  (`ui-density`, `ui-craft-polish`, `ui-systems`, `ui-content`, `ui-motion-micro`).
- Experts participate in **all phases** (round 1 + round 2 votes), not optional add-ons.
- Slash command: **`/uxui-panel`** (this `/ux-panel` name remains as a trigger alias).

Immediately open and execute the full workflow in `uxui-panel/SKILL.md` and
`uxui-panel/references/lens-briefs.md`.
